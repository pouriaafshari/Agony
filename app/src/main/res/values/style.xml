<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/color_pressed</item>
        <item name="colorPrimaryDark">@color/rectangle_solid</item>
        <item name="colorAccent">@color/color_background</item>
        <item name="android:windowBackground">@drawable/gradientbg</item>
    </style>
</resources>
